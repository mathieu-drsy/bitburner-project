/** @param {NS} ns */
export async function doom(ns) {
	//your turn bozo
	//https://codepen.io/nodws/pen/VWOLPZ

}
/*
const element = document.getElementById("pid" + ns.pid);
	if (element != null) {
		element.remove();
	}
	document.getElementById("terminal").insertAdjacentHTML('beforeend', `<li><div id="pid` + ns.pid + `">`+ doom +`</div></li>`);
*/